{
  "mcpServers": {
    "agent-brain": {
      "type": "sse",
      "url": "${NIGHTHAWK_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${NIGHTHAWK_JWT}",
        "X-Agent-ID": "${NIGHTHAWK_AGENT_ID}"
      }
    }
  }
}
