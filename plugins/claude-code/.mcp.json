{
  "mcpServers": {
    "agent-brain": {
      "type": "sse",
      "url": "${NIGHTHAWK_MCP_URL}",
      "headers": {
        "X-API-Key": "${NIGHTHAWK_API_KEY}",
        "X-Agent-ID": "${NIGHTHAWK_AGENT_ID}"
      }
    }
  }
}
